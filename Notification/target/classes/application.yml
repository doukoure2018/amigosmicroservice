server:
  port: 8083
spring:
  cloud:
    config:
      enabled: false
  application:
    name: NOTIFICATION
  datasource:
    password: doukoure2711
    url: jdbc:postgresql://localhost:5432/notification
    username: amigoscode
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


  sleuth:
    tracer:
      enabled: true  # Enable tracing (Micrometer Tracing replaces Sleuth)
  zipkin:
    enabled: true  # Enable sending traces to Zipkin
    base-url: http://localhost:9411/
  management:
    endpoints:
      web:
        exposure:
          include: "*"
    tracing:
      enabled: true
  logging:
    level:
      org.springframework.cloud.openfeign: DEBUG


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
